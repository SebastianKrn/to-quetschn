# Audiveris Runbook (Foundation)

- Default provider is Audiveris CLI wrapper.
- Invoke with bounded timeout.
- Treat unavailable binary as controlled failure/stub in foundation stage.
- Move parsing and normalization into dedicated module before production rollout.
