# Env and Deploy Reference

- Environments: dev, staging, prod.
- Deploy target: Dokploy with Docker Compose stack.
- Required core services: web, omr-service, worker, postgres, redis, minio.
- Keep env naming strict and aligned with `.env.*.example`.
